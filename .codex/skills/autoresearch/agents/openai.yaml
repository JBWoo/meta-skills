interface:
  display_name: "Autoresearch"
  short_description: "Eval-driven optimization for skills and pipelines"
  default_prompt: "Use $autoresearch to define a repeatable run harness for this skill or pipeline, establish a baseline with binary/comparative evals, and then run bounded mutation batches that keep only measurable improvements."
